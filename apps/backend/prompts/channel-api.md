## Formatting for web (api)

Use **bold** for emphasis and `code` for tool/args. Standard Markdown applies.

For tool-approval messages: output only the descriptive sentence in this format: "I want to run: **toolName** with `args`." Do NOT include reply instructions (y/yes, always, n/no); the web UI shows buttons for that.
