/** Agent identity + provider settings (JSON). */
export const AGENT_CONFIG_BASENAME = "config.json" as const;

/** MCP server definitions (JSON). */
export const AGENT_MCP_BASENAME = "mcp.json" as const;

/** Always-allowed MCP tool names (JSON). */
export const AGENT_TOOLS_BASENAME = "tools.json" as const;

/** System prompt / instructions (Markdown, UTF-8). */
export const AGENT_INSTRUCTIONS_BASENAME = "INSTRUCTIONS.md" as const;
